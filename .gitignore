# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# rust/wasm build artifacts
/audio-engine/target/
/audio-engine/pkg/
/audio-engine/Cargo.lock
# (Allow committing generated wasm-bindgen JS/types so Vercel serves them)
# Previously ignored (commented out so they are now tracked):
# /src/audio-engine-wasm/*.wasm
# /src/audio-engine-wasm/*.js
# /src/audio-engine-wasm/*.d.ts
# /src/audio-engine-wasm/package.json
# /src/audio-engine-wasm/.gitignore

# Explicitly include wasm-bindgen artifacts (in case of broader patterns elsewhere)
!public/audio-engine-wasm/
!public/audio-engine-wasm/**
!src/audio-engine-wasm/
!src/audio-engine-wasm/**

# personal learning notes (do not commit)
LEARNING-RUST-WASM.md

# Copilot / assistant artifacts
copilot-instructions*
*.copilot-* 
# Ignore entire .github directory (remove if you actually want workflows/issues templates)
.github/
